apiVersion: v1
kind: Secret
metadata:
  name: {{ include "java-service.name" . }}-secret
type: Opaque
stringData:
{{- if .Values.secretEnv }}
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- else }}
  {}
{{- end }}
