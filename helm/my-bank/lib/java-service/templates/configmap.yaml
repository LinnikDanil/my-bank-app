apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "java-service.name" . }}-config
data:
{{- if .Values.env }}
{{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- else }}
  {}
{{- end }}
